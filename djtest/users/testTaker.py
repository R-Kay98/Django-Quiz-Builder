class student:
	def __init__(self, QID, question, qNum, numCorrect, choices, shortAnswer):
		self.QID = QID
		self.question = question
		self.qNum = qNum
		self.numCorrect = numCorrect
		self.choices = choices
		self.shortAnswer = shortAnswer